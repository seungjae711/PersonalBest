<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group12_project">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".chat.chatActivity"
            android:label="@string/title_activity_chat"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".friendlist.FriendListActivity"
            android:label="Friends"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".set_goal.RecommendedGoal"
            android:label="Recommend Goal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".set_goal.CustomGoal"
            android:label="Custom Goal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".set_goal.GoalDialog"
            android:configChanges="orientation"
            android:screenOrientation="sensor"
            android:theme="@style/NoTitleDialog" />
        <activity
            android:name=".StepChart"
            android:label="Step Chart"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HeightEmailManager"
            android:label="Height Manager"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".StatsDialog"
            android:label="Stats Dialog"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>